/**
 * LLM Provider Abstraction Layer
 * Unified interface for multiple LLM providers
 *
 * Full implementation in Epic 3
 * @module grooveagent-node/llm
 */

const VERSION = '1.0.0';
const placeholder = true;

/**
 * LLM Provider interface placeholder
 * @typedef {Object} LLMProvider
 * @property {Function} complete - Send completion request
 * @property {Function} isAvailable - Check provider availability
 */

module.exports = {
  VERSION,
  placeholder
};
