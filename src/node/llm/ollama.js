/**
 * Ollama Provider
 * Primary LLM provider (FREE, local)
 *
 * Full implementation in Story 2.3
 * @module grooveagent-node/llm/ollama
 */

const VERSION = '1.0.0';
const placeholder = true;
const PROVIDER_NAME = 'ollama';

module.exports = {
  VERSION,
  placeholder,
  PROVIDER_NAME
};
