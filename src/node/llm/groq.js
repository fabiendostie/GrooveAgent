/**
 * Groq Provider
 * Speed fallback LLM provider (ultra-fast)
 *
 * Full implementation in Story 3.4
 * @module grooveagent-node/llm/groq
 */

const VERSION = '1.0.0';
const placeholder = true;
const PROVIDER_NAME = 'groq';

module.exports = {
  VERSION,
  placeholder,
  PROVIDER_NAME
};
