/**
 * OpenAI Provider
 * Alternative LLM provider
 *
 * Full implementation in Story 3.3
 * @module grooveagent-node/llm/openai
 */

const VERSION = '1.0.0';
const placeholder = true;
const PROVIDER_NAME = 'openai';

module.exports = {
  VERSION,
  placeholder,
  PROVIDER_NAME
};
