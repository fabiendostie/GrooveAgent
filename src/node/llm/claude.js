/**
 * Claude Provider
 * Quality fallback LLM provider
 *
 * Full implementation in Story 3.2
 * @module grooveagent-node/llm/claude
 */

const VERSION = '1.0.0';
const placeholder = true;
const PROVIDER_NAME = 'claude';

module.exports = {
  VERSION,
  placeholder,
  PROVIDER_NAME
};
