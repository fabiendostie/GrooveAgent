/**
 * Timing Module
 * Swing and micro-timing transformations
 *
 * Full implementation in Story 2.6
 * @module grooveagent-node/midi/timing
 */

const VERSION = '1.0.0';
const placeholder = true;

/**
 * Default swing ratio (straight time)
 * @constant {number}
 */
const DEFAULT_SWING_RATIO = 0.5;

module.exports = {
  VERSION,
  placeholder,
  DEFAULT_SWING_RATIO
};
