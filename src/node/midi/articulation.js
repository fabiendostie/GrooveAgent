/**
 * Articulation Module
 * Note length and overlap transformations
 *
 * Full implementation in Story 2.6
 * @module grooveagent-node/midi/articulation
 */

const VERSION = '1.0.0';
const placeholder = true;

/**
 * Default note length factor
 * @constant {number}
 */
const DEFAULT_NOTE_LENGTH_FACTOR = 1.0;

module.exports = {
  VERSION,
  placeholder,
  DEFAULT_NOTE_LENGTH_FACTOR
};
